ext {

    def versionMajor = 1
    def versionMinor = 2
    def versionPatch = 4
    TESTCORE_LIB_PUBLISH_VERSION = "$versionMajor.$versionMinor.$versionPatch"

    def uiVersionMajor = 1
    def uiVersionMinor = 3
    def uiversionPatch = 1
    TESTUI_LIB_PUBLISH_VERSION = "$uiVersionMajor.$uiVersionMinor.$uiversionPatch"
}